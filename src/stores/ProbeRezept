// store.js
import { defineStore } from 'pinia';

export const useRecipeStore = defineStore({
  id: 'recipe',
  state: () => ({
    recipe: {
      title: 'Leckeres Rezept',
      ingredients: ['Kartoffel', 'Wasser', 'Salz'],
    },
  }),
});
